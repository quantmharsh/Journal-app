# write IP
spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=journaldb
# since we want unique username for all users  so we need to mark it as true. just by adding @Indexed (unique=true)
# it will not work . we also need to do this
spring.data.mongodb.auto-index-creation=true
# when we have username and password that we use to connect to db 
# spring.data.mongodb.username=harsh
# spring.data.mongodb.password=harsh122